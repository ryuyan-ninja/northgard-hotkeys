;
; House
;
#If WinActive("Northgard") and build_active
q::
    if ClickImage("house.png")
        build_active := false
return

;
; Scout Camp
;
#If WinActive("Northgard") and build_active
w::
    if ClickImage("scout.png")
        build_active := false
return

;
; Woodcutter's Lodge
;
#If WinActive("Northgard") and build_active
e::
    if ClickImage("woodcutter.png")
        build_active := false
return

;
; Healer's Hut
;
#If WinActive("Northgard") and build_active
r::
    if ClickImage("healer.png")
        build_active := false
return

;
; Fisherman's Hut
;
#If WinActive("Northgard") and build_active
a::
    if ClickImage("fisherman.png")
        build_active := false
return

;
; Hunter's Lodge
;
#If WinActive("Northgard") and build_active
d::
    if ClickImage("hunter.png")
        build_active := false
return

;
; Food Silo
;
#If WinActive("Northgard") and build_active
f::
    if ClickImage("food.png")
        build_active := false
return

;
; Trading Post
;
#If WinActive("Northgard") and build_active
z::
    if ClickImage("trading.png")
        build_active := false
return

;
; Marketplace
;
#If WinActive("Northgard") and build_active
x::
    if ClickImage("market.png")
        build_active := false
return

;
; Longship Dock
;
#If WinActive("Northgard") and build_active
c::
    if ClickImage("dock.png")
        build_active := false
return

;
; Hall of Skalds
;
#If WinActive("Northgard") and build_active
v::
    if ClickImage("skald.png")
        build_active := false
return

;
; Mine
;
#If WinActive("Northgard") and build_active
+z::
    if ClickImage("mine.png")
        build_active := false
return

;
; Forge
;
#If WinActive("Northgard") and build_active
+x::
    if ClickImage("forge.png")
        build_active := false
return

;
; Carved Stone
;
#If WinActive("Northgard") and build_active
+c::
    if ClickImage("stone.png")
        build_active := false
return

;
; Training Camp
;
#If WinActive("Northgard") and build_active
1::
    if ClickImage("training.png")
        build_active := false
return

;
; Axe Thrower Camp
;
#If WinActive("Northgard") and build_active
2::
    if ClickImage("axe.png")
        build_active := false
return

;
; Shield Bearer Camp
;
#If WinActive("Northgard") and build_active
3::
    if ClickImage("shield.png")
        build_active := false
return

;
; Defensive Tower
;
#If WinActive("Northgard") and build_active
4::
    if ClickImage("tower.png")
        build_active := false
return

;
; Carved Statue
;
#If WinActive("Northgard") and build_active
+1::
    if ClickImage("statue.png")
        build_active := false
return

;
; Altar of Kings
;
#If WinActive("Northgard") and build_active
+2::
    if ClickImage("altar.png")
        build_active := false
return

;
; Lighthouse
;
#If WinActive("Northgard") and build_active
+3::
    if ClickImage("lighthouse.png")
        build_active := false
return